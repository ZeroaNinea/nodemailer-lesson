{
  "name": "nodemailer-lesson",
  "version": "1.0.0",
  "description": "Sending emails in Node.js using Nodemailer — lesson.",
  "keywords": [
    "nodemailer",
    "node.js",
    "lesson"
  ],
  "license": "ISC",
  "author": "0a9a",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "start:watch": "nodemon dist/index.js",
    "start:dev": "nodemon src/index.ts",
    "build": "npx tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@types/nodemailer": "^8.0.1",
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "nodemailer": "^9.0.1"
  }
}
